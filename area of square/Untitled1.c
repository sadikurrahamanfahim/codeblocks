#include<stdio.h>
int main()
{
    int area,length;
    printf("Enter length of square: ");
    scanf("%d",&length);
    area=length*length;

    printf("Area of square is: %d", area);
    return 0;
}

